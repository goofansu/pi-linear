# pi-linear

Interact with Linear issues, comments, labels, projects, teams, and user profiles from Pi.

## Install

```bash
pi install https://github.com/goofansu/pi-linear
```

After installation, Pi registers the `linear` tool.

## Configuration

Set `LINEAR_API_KEY` to a Linear personal API key from <https://linear.app/settings/api>.
